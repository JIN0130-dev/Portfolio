



Claim offer
I am gonna make portfolio with react.
give me a excellent sample with images.

Here is a very good portfolio layout example you can build with React. I’ll show you:

Real design inspiration (images)

Recommended sections

React project structure

Simple starter code

1. Portfolio Design Inspiration
Modern Developer Portfolio (Dark Theme)

https://www.shadcndesign.com/_next/image?dpl=dpl_BVvW3JFWxyvdR7zQZwowQvRwojwM&q=75&url=%2Ftemplates%2Fdev-1.png&w=3840

https://cdn.prod.website-files.com/5e8e816d43060db856099187/63b325e620d72639be415d63_2-dark-mode-design-portfolio-webflow-template-1.5x.png

https://cdn.dribbble.com/userupload/36728116/file/original-c1710fc847b10b6da0bf49fc3dd8259b.png?resize=752x&vertical=center
4
Common features used in modern portfolios:

Dark theme

Big hero section

Project cards

Skill icons

Smooth animations

Minimal layout

These are widely used in modern developer portfolios. 

2. Recommended Portfolio Sections
A professional developer portfolio usually contains:

1️⃣ Hero Section
Name

Title

Short intro

Buttons (Contact / Projects)

Profile image

Example:

Hi, I'm John Doe
Senior Full Stack Developer

I build scalable web applications using React, Next.js, and Python.

[View Projects]   [Contact Me]
2️⃣ About Me
About Me

I am a Full Stack Developer with 8+ years of experience.
I specialize in React, Next.js, Node.js, and cloud systems.

I love building scalable applications and solving complex problems.
3️⃣ Skills Section
Example grid:

React
Next.js
Node.js
Python
PostgreSQL
AWS
Docker
TypeScript
Use icons from:

react-icons

devicons

4️⃣ Projects Section
Example cards:

E-commerce Platform
React + Node + Stripe
[Live Demo] [Github]

Task Management App
Next.js + Supabase
[Live Demo] [Github]
5️⃣ Experience
Example:

Senior Software Engineer
Tech Company
2022 — Present

Full Stack Developer
Startup
2020 — 2022
6️⃣ Contact Section
Email
LinkedIn
GitHub
Twitter
3. React Portfolio Project Structure
Example structure:

portfolio
 ├─ public
 │   └─ profile.jpg
 │
 ├─ src
 │   ├─ components
 │   │   ├─ Navbar.jsx
 │   │   ├─ Hero.jsx
 │   │   ├─ About.jsx
 │   │   ├─ Skills.jsx
 │   │   ├─ Projects.jsx
 │   │   └─ Contact.jsx
 │   │
 │   ├─ App.jsx
 │   └─ main.jsx
 │
 └─ package.json
4. Hero Component Example
export default function Hero() {
  return (
    <section className="hero">
      <div className="hero-left">
        <h1>Hi, I'm John Doe</h1>
        <h2>Senior Full Stack Developer</h2>
        <p>
          I build scalable web apps with React, Next.js and Python.
        </p>

        <button>View Projects</button>
        <button>Contact Me</button>
      </div>

      <div className="hero-right">
        <img src="/profile.jpg" alt="profile"/>
      </div>
    </section>
  )
}
5. Project Card Example
export default function ProjectCard({title, tech}) {
  return (
    <div className="card">
      <h3>{title}</h3>
      <p>{tech}</p>

      <div className="links">
        <a href="#">Live</a>
        <a href="#">Github</a>
      </div>
    </div>
  )
}
6. Best Libraries for React Portfolio
I strongly recommend using:

UI
Tailwind CSS

shadcn/ui

Material UI

Animation
framer-motion

Icons
react-icons

Effects
particles.js

three.js (optional)

7. Advanced Features (To Make Portfolio Excellent)
Add these to stand out:

✅ Smooth scroll
✅ Dark/light mode
✅ Project animations
✅ GitHub stats
✅ Blog section
✅ Download resume
✅ Project filtering

8. Example Tech Stack (Best for 2026)
Since you focus on:

JavaScript

TypeScript

Python

React / Next.js

Use this stack:

React + Vite
TailwindCSS
Framer Motion
React Icons
EmailJS
💡 If you want, I can also give you:

A complete React portfolio template (copy-paste ready)

A very modern developer portfolio like top engineers use

A GitHub-level portfolio layout recruiters love

Just tell me and I'll generate it.


so, give me a complete code and process

I’ll give you a clean modern React portfolio (complete guide + code).
Stack: React + Vite + Tailwind + Framer Motion + React Icons — fast and modern.

1️⃣ Create the React Project
Open terminal.

npm create vite@latest my-portfolio
Select:

React
JavaScript
Go to project:

cd my-portfolio
npm install
Run project:

npm run dev
2️⃣ Install Required Libraries
npm install tailwindcss @tailwindcss/vite
npm install framer-motion
npm install react-icons
npm install react-scroll
3️⃣ Setup Tailwind
Create Tailwind config:

npx tailwindcss init
Update tailwind.config.js

export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}
Update src/index.css

@tailwind base;
@tailwind components;
@tailwind utilities;

body{
  @apply bg-black text-white;
}
4️⃣ Project Structure
src
 ├ components
 │   ├ Navbar.jsx
 │   ├ Hero.jsx
 │   ├ About.jsx
 │   ├ Skills.jsx
 │   ├ Projects.jsx
 │   └ Contact.jsx
 │
 ├ App.jsx
 ├ main.jsx
 └ index.css
5️⃣ Navbar Component
src/components/Navbar.jsx

import { Link } from "react-scroll";

export default function Navbar() {
  return (
    <nav className="fixed w-full bg-black shadow-md z-50">
      <div className="max-w-6xl mx-auto flex justify-between p-4">

        <h1 className="text-xl font-bold">MyPortfolio</h1>

        <div className="space-x-6">
          <Link to="about" smooth={true}>About</Link>
          <Link to="skills" smooth={true}>Skills</Link>
          <Link to="projects" smooth={true}>Projects</Link>
          <Link to="contact" smooth={true}>Contact</Link>
        </div>

      </div>
    </nav>
  );
}
6️⃣ Hero Section
src/components/Hero.jsx

import { motion } from "framer-motion";

export default function Hero() {
  return (
    <section className="h-screen flex items-center justify-center text-center">

      <motion.div
        initial={{opacity:0,y:50}}
        animate={{opacity:1,y:0}}
        transition={{duration:1}}
      >
        <h1 className="text-5xl font-bold mb-4">
          Hi, I'm John Doe
        </h1>

        <p className="text-xl mb-6">
          Full Stack Developer
        </p>

        <button className="bg-blue-500 px-6 py-3 rounded-lg">
          View Projects
        </button>
      </motion.div>

    </section>
  );
}
7️⃣ About Section
src/components/About.jsx

export default function About(){
  return(
    <section id="about" className="py-20 max-w-5xl mx-auto">

      <h2 className="text-4xl font-bold mb-6">About Me</h2>

      <p className="text-lg text-gray-300">
        I am a Full Stack Developer with strong experience in
        React, Next.js, Node.js and Python. I love building
        scalable web applications and solving complex problems.
      </p>

    </section>
  )
}
8️⃣ Skills Section
src/components/Skills.jsx

import { FaReact, FaNodeJs, FaPython } from "react-icons/fa";
import { SiNextdotjs, SiPostgresql } from "react-icons/si";

export default function Skills(){
  return(

    <section id="skills" className="py-20 bg-gray-900">

      <div className="max-w-6xl mx-auto text-center">

        <h2 className="text-4xl font-bold mb-10">Skills</h2>

        <div className="grid grid-cols-3 md:grid-cols-5 gap-10 text-5xl">

          <FaReact/>
          <SiNextdotjs/>
          <FaNodeJs/>
          <FaPython/>
          <SiPostgresql/>

        </div>

      </div>

    </section>
  )
}
9️⃣ Projects Section
src/components/Projects.jsx

export default function Projects(){

  const projects = [
    {
      title:"Ecommerce App",
      tech:"React + Node"
    },
    {
      title:"Task Manager",
      tech:"Next.js + Supabase"
    },
    {
      title:"Chat App",
      tech:"React + Firebase"
    }
  ]

  return(

    <section id="projects" className="py-20 max-w-6xl mx-auto">

      <h2 className="text-4xl font-bold text-center mb-12">
        Projects
      </h2>

      <div className="grid md:grid-cols-3 gap-8">

        {projects.map((p,i)=>(
          <div key={i} className="bg-gray-800 p-6 rounded-xl">

            <h3 className="text-xl font-bold mb-2">
              {p.title}
            </h3>

            <p className="text-gray-400">
              {p.tech}
            </p>

            <button className="mt-4 text-blue-400">
              View Project
            </button>

          </div>
        ))}

      </div>

    </section>
  )
}
🔟 Contact Section
src/components/Contact.jsx

export default function Contact(){
  return(

    <section id="contact" className="py-20 bg-gray-900">

      <div className="max-w-xl mx-auto text-center">

        <h2 className="text-4xl font-bold mb-6">
          Contact Me
        </h2>

        <p className="mb-6">
          email@example.com
        </p>

        <button className="bg-blue-500 px-6 py-3 rounded-lg">
          Send Message
        </button>

      </div>

    </section>
  )
}
11️⃣ App.jsx
src/App.jsx

import Navbar from "./components/Navbar"
import Hero from "./components/Hero"
import About from "./components/About"
import Skills from "./components/Skills"
import Projects from "./components/Projects"
import Contact from "./components/Contact"

function App() {

  return (
    <>
      <Navbar/>
      <Hero/>
      <About/>
      <Skills/>
      <Projects/>
      <Contact/>
    </>
  )
}

export default App
12️⃣ Run Portfolio
Start project:

npm run dev
Open:

http://localhost:5173
13️⃣ Deploy Portfolio (Recommended)
Best platforms:

Vercel

Netlify

GitHub

Example deploy with Vercel:

npm install -g vercel
vercel
🚀 If you want, I can also give you
🔥 A MUCH BETTER portfolio (senior-level design)

Animated React portfolio (Framer Motion + 3D)

Portfolio like top developers in 2026

GitHub-ready complete portfolio repo

Just tell me and I’ll generate a 🔥 10x better portfolio template.


give me the sample image of this code' s result


Image created
•
Personal portfolio website screenshot
Generated image
Generated image
Generated image



Share

this is very simple, 
"Steven Morales
Miami, Florida, United States w.bexpert5007@gmail.com +1 (929) 810 3425 in/stmorales23
SUMMARY
Versatile and results-driven Senior Full Stack Engineer with 9 years of progressive experience delivering scalable, performant, and secure web,
cloud, and mobile solutions. Demonstrated leadership in mentoring engineers, architecting full-stack systems, and aligning development with
business goals. Adept in cloud-native development (AWS, GCP), CI/CD pipelines, microservices architecture, containerization with
Docker/Kubernetes and agile DevOps practices. Proficient in a wide range of technologies, including React, Vue, Angular, Node, Express, PHP,
Python, Django, Java(Spring Boot, Spring MVC, J2EE), .NET and C#, with a commitment to delivering high-quality, maintainable code.
EDUCATION
Master of Science - MS, Data Science
Florida International University • Florida, US • 2024 • 3.7
Bachelor of Science - BS, Computer Science
Florida International University • Florida, US • 2020
EXPERIENCE
Senior Software Developer
Norwegian Cruise Line Holdings Ltd. (NCLH) August 2024 - September 2025, Miami, Florida, US
• Delivered backend and full-stack features supporting high-traffic, business-critical websites such as corporate communications, investor
reporting, and cruise booking experiences for Norwegian Cruise Line, Oceania Cruises, and Regent Seven Seas Cruises.
• Built and maintained scalable backend services and APIs using Scala, Node.js, Python (Django), and ASP.NET / C#, ensuring reliability,
performance, and secure data exchange across distributed systems.
• Worked across multiple frontend stacks (React, Vue, Angular) to support modern, responsive user interfaces for corporate content, investor
portals, and customer-facing brand sites.
• Designed and implemented an API Proxy layer to streamline communication between shipboard systems and shore-side services, reducing
latency by approximately 33% and improving overall system efficiency.
• Engineered asynchronous communication pipelines using Kafka and RabbitMQ, implementing event driven architectures to enable real-time data
processing, which decreased response times by 25%.
• Streamlined CI/CD pipelines with GitHub Actions, GitLab CI/CD, and Docker/Kubernetes, reducing deployment times by 50%, leading to faster
release cycles and quicker time-to-market for new features.
• Managed cloud deployments on AWS EC2, Elastic Beanstalk and Google Cloud Compute Engine, utilizing IAM roles, security groups, and
encryption to ensure secure, scalable application hosting and data protection.
• Tech Stack: Scala, Java, Node.js, Python, Django, ASP.NET Core, C#, React, Vue, Angular, REST APIs, Distributed Systems.
Multi-Role Technical Experience
Florida International University (FIU) January 2018 - July 2024, Miami, Florida, US
• Progressed through multiple technical and analytical roles at Florida International University, contributing to enterprise ERP systems, web
platforms, data analytics, and campus IT operations. Delivered reliable data solutions, improved digital platforms, and supported business-critical
academic and administrative workflows.
• Designed, optimized, and maintained complex SQL queries and reporting logic within ERP and HR systems, significantly improving data
accuracy, performance, and decision-making for HR and finance stakeholders.
• Analyzed, enhanced, and supported PeopleSoft 9.2 modules across HR, payroll, and financial aid domains, ensuring data integrity, auditability, and
compliance with institutional policies.
• Developed and maintained university-wide web platforms supporting online learning, institutional communications, and digital content delivery.
• Improved UI/UX, accessibility (WCAG), SEO, and interactivity across multiple university web properties, increasing usability and engagement.
• Built and supported internal tools for digital media content management, analytics, and reporting, enabling data-driven insights for
departments.
• Maintained and enhanced scholarship and financial aid database systems, producing reconciliation and audit reports covering all scholarship
accounts.
• Provided technical support and workflow automation through Salesforce, supporting departmental operations and service requests.
• Analyzed parking garage sensor data to improve accuracy and operational reporting.
• Managed departmental websites, including content updates, plugins, and performance optimizations.
• Tech Stacks: HTML, CSS, JavaScript, TypeScript, SASS, TailwindCSS, React, Next, Vue, Nuxt, Angular, Node, Express.js, Nest, Redux, Vuex, Three.js
PHP, Laravel, Java, React Native, Swift, Flutter, Kotlin, Python, Django, Flask, FastAPI, C#, .NET, ASP.NET, Ruby on Rails, Golang, MySQL, PostgreSQL,
MongoDB.PROJECT
Senior Software Engineer
NCLH • www.nclhltd.com/
• Corporate / Holding Company Platform (Corporate Content, Sustainability, Governance).
• Built and enhanced corporate web experiences with strong SEO, accessibility, and content governance, integrating brand links and corporate
disclosures.
• Implemented scalable APIs and content delivery patterns to support global traffic, fast page loads, and reliable publishing workflows.
• Supported observability and release pipelines for high-availability production deployments.
• Delivered investor-facing experiences with strong reliability and auditability, ensuring accurate and timely publication of financial/regulatory
materials (https://www.nclhltd.com/investors).
• Implemented secure, cache-friendly delivery for peak-event traffic (earnings releases, announcements) and ensured content integrity.
• Technology Stack: React + Next.js (SSR/ISR), TypeScript, Tailwind CSS or Sass, React Query, Redux Toolkit, React Hook Form, Zod/Yup, Node.js
(NestJS/Express), Python (Django), Redis, OAuth2, Elastic/OpenSearch (SEO), Docker, GitHub Actions/Jenkins, Datadog.
Senior Software Engineer
Norwegian Cruise Line • www.ncl.com/
• Norwegian Cruise Line – Consumer Booking Platform (Itineraries, Ships, Booking Flows).
• Contributed to conversion-sensitive consumer experiences supporting itinerary discovery, ship pages, pricing, and booking/checkout flows.
• Integrated downstream services (availability, pricing, promotions, identity) using resilient API patterns and consistent UX performance.
• Technology Stack: Angular, TypeScript, NgRx, Node.js (NestJS), Scala/Java, Stripe, Apple Pay, Google Pay, Cypress E2E, OpenAPI/Swagger.
Software Developer
AI Budgeting & Financial Insights Application • February 2025 - January 2026
• Designed and developed an AI-powered budgeting and personal finance application enabling users to track expenses, analyze spending behavior,
and receive predictive insights for financial planning.
• Built a cross-platform mobile interface using React Native with a focus on usability, performance, and real-time data updates.
• Implemented backend communication using gRPC for efficient, low-latency data exchange between mobile clients and backend services.
• Integrated Firebase for authentication, real-time data synchronization, cloud storage, and push notifications.
• Applied machine learning models to categorize expenses, forecast spending trends, and generate personalized financial recommendations.
• Technology Stack: React Native, TypeScript, gRPC, Firebase (Auth, Firestore, Cloud Functions), Python, Machine Learning, REST/gRPC APIs.
AI Engineer
Music Generation Using LSTM & CNN Models • January 2024 - July 2024
• Designed and trained deep learning models combining Long Short-Term Memory (LSTM) networks and Convolutional Neural Networks (CNN) to
generate and predict musical note sequences.
• Experimented with varying sequence lengths, model architectures, and feature representations to optimize generative performance.
• Evaluated model accuracy and generative quality through quantitative metrics, achieving an F1-score of 87.57% with a convolutional LSTM
architecture.
• Conducted systematic data preprocessing, feature engineering, and hyperparameter tuning to improve prediction consistency.
• Technology Stack: Python, TensorFlow, Keras, NumPy, Pandas, LSTM, CNN, Deep Learning, Model Evaluation.
AI Developer
Mood-Based Music Recommendation System • January 2024 - March 2024
• Developed a sentiment-driven recommendation engine that maps user mood and text input to music recommendations.
• Leveraged the Twitter API to collect real-time textual data and applied natural language processing (NLP) techniques for sentiment extraction.
• Implemented TF-IDF vectorization and machine learning classifiers to compute sentiment polarity and intensity.
• Matched user sentiment against a dataset of 114,000 Spotify tracks, delivering personalized song recommendations aligned with emotional
context.
• Technology Stack: Python, NLP, Sentiment Analysis, TF-IDF, Scikit-learn, Twitter API, Spotify Dataset, Data Modeling.
CERTIFICATIONS
Deep Learning Specialization
DeepLearning.AI • 2024
Structuring Machine Learning Projects
DeepLearning.AI • 2024
Learning Django
LinkedIn • 2020
IBM Data Science Professional CertificateCoursera • 2019
SKILLS
Languages & Frameworks: HTML, CSS, JavaScript, TypeScript, SASS, TailwindCSS, React, Next, Vue, Nuxt, Angular, Node, Express.js, Nest, Redux,
Vuex, Three.js PHP, Laravel, Java, React Native, Swift, Flutter, Kotlin, Python, Django, Flask, FastAPI, C#, .NET, ASP.NET, Ruby on Rails, Golang, MUI
Databases: MySQL, PostgreSQL, SQLite, Redis, ElasticSearch, MongoDB, DynamoDB, Microsoft SQL Server, Firebase Firestore
Software Skills: Frontend, Backend, DB, Amazon Web Services ( S3 | EC2 | RDS | Lambda | IAM ), Microsoft Azure | Azure, Google Cloud Platform
(GCP), Docker, Kubernetes, IaC (Terraform), DevOps, CI / CD ( Jenkins | GitLab CI ), Git ( GitHub | GitLab | Bitbucket ), Unit Testing ( Jest | Mocha |
PyTest | JUnit | NUnit ), ML | Machine Learning ( Keras | TensorFlow | PyTorch | NumPy | Pandas )
Tools & Other: HTTP/HTTPS, Cypress, Selenium, Postman, DNS, WebSockets, VPN, Slack, OAuth2.0, JWT, SSL/TLS, OpenAI API, Jira, Trello, Asana,
Xcode, PyCharm, Visual Studio Code | VSCode, FreeSwitch"

this is my resume.
I am going to make Portfolio about my resume. and I am going to insert my Picture in About page.
give me a very wonderful and great and execellent style code.

Perfect! Since you want a senior-level, modern, professional portfolio based on your resume with your picture in the About section, I’ll craft a full React + Tailwind + Framer Motion portfolio template that looks like something a top engineer would have. It will be sleek, modern, interactive, dark/light theme, and fully responsive.

I’ll also highlight your skills, experience, projects, AI work, and certifications with elegant cards and sections.

1️⃣ Project Setup
# Create React project
npm create vite@latest steven-portfolio
cd steven-portfolio
npm install
npm install tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm install framer-motion react-icons react-scroll
tailwind.config.js

export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        primary: "#1DA1F2",
        dark: "#0F0F0F",
        light: "#F5F5F5"
      }
    },
  },
  plugins: [],
}
src/index.css

@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-dark text-light font-sans;
}
2️⃣ Project Structure
src/
 ├ components/
 │    ├ Navbar.jsx
 │    ├ Hero.jsx
 │    ├ About.jsx
 │    ├ Skills.jsx
 │    ├ Experience.jsx
 │    ├ Projects.jsx
 │    ├ Certifications.jsx
 │    ├ Contact.jsx
 │    └ Footer.jsx
 ├ assets/
 │    └ steven.jpg      <-- your picture here
 ├ App.jsx
 └ main.jsx
3️⃣ Navbar.jsx
import { Link } from "react-scroll";
import { FaSun, FaMoon } from "react-icons/fa";
import { useState } from "react";

export default function Navbar() {
  const [darkMode, setDarkMode] = useState(true);

  const toggleDarkMode = () => {
    setDarkMode(!darkMode);
    document.body.classList.toggle("bg-dark");
    document.body.classList.toggle("bg-light");
    document.body.classList.toggle("text-dark");
    document.body.classList.toggle("text-light");
  }

  return (
    <nav className="fixed w-full bg-dark/90 backdrop-blur-md z-50">
      <div className="max-w-6xl mx-auto flex justify-between items-center p-4">
        <h1 className="text-2xl font-bold">Steven Morales</h1>
        <div className="flex items-center gap-6">
          <Link to="about" smooth={true} className="cursor-pointer">About</Link>
          <Link to="skills" smooth={true} className="cursor-pointer">Skills</Link>
          <Link to="experience" smooth={true} className="cursor-pointer">Experience</Link>
          <Link to="projects" smooth={true} className="cursor-pointer">Projects</Link>
          <Link to="contact" smooth={true} className="cursor-pointer">Contact</Link>
          <button onClick={toggleDarkMode}>
            {darkMode ? <FaSun /> : <FaMoon />}
          </button>
        </div>
      </div>
    </nav>
  )
}
4️⃣ Hero.jsx
import { motion } from "framer-motion";

export default function Hero() {
  return (
    <section className="h-screen flex flex-col justify-center items-center text-center px-6">
      <motion.h1 
        className="text-5xl md:text-6xl font-bold mb-4"
        initial={{ opacity: 0, y: -50 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 1 }}
      >
        Hi, I'm Steven Morales
      </motion.h1>
      <motion.p
        className="text-xl md:text-2xl mb-6 max-w-2xl"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.5, duration: 1 }}
      >
        Senior Full Stack Engineer | Cloud & AI Expert | 9 Years of Experience Building Scalable Web & Mobile Solutions
      </motion.p>
      <motion.button
        className="bg-primary text-dark px-6 py-3 rounded-lg font-semibold hover:scale-105 transition"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 1, duration: 1 }}
      >
        View My Work
      </motion.button>
    </section>
  )
}
5️⃣ About.jsx (with Picture)
import stevenPic from "../assets/steven.jpg";
import { motion } from "framer-motion";

export default function About() {
  return (
    <section id="about" className="py-20 max-w-6xl mx-auto flex flex-col md:flex-row items-center gap-10 px-6">
      
      <motion.img 
        src={stevenPic} 
        alt="Steven Morales" 
        className="w-64 h-64 rounded-full object-cover border-4 border-primary"
        initial={{ opacity: 0, x: -100 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 1 }}
      />
      
      <motion.div
        initial={{ opacity: 0, x: 100 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 1 }}
      >
        <h2 className="text-4xl font-bold mb-4">About Me</h2>
        <p className="text-gray-300 text-lg mb-4">
          Versatile and results-driven Senior Full Stack Engineer with 9 years of experience delivering scalable, performant, and secure web, cloud, and mobile solutions.
        </p>
        <p className="text-gray-300 text-lg">
          Skilled in React, Next.js, Node.js, Python, Django, Angular, Vue, Java(Spring Boot), .NET/C#, PHP, Laravel, AI/ML, Cloud(AWS/GCP), DevOps, and more.
        </p>
      </motion.div>

    </section>
  )
}
6️⃣ Skills.jsx
import { FaReact, FaNodeJs, FaPython, FaDocker, FaAws } from "react-icons/fa";
import { SiNextdotjs, SiAngular, SiVueDotJs, SiKubernetes } from "react-icons/si";

export default function Skills() {
  const skills = [
    { icon: <FaReact />, name: "React" },
    { icon: <SiNextdotjs />, name: "Next.js" },
    { icon: <FaNodeJs />, name: "Node.js" },
    { icon: <FaPython />, name: "Python" },
    { icon: <SiAngular />, name: "Angular" },
    { icon: <SiVueDotJs />, name: "Vue.js" },
    { icon: <FaDocker />, name: "Docker" },
    { icon: <SiKubernetes />, name: "Kubernetes" },
    { icon: <FaAws />, name: "AWS" },
  ];

  return (
    <section id="skills" className="py-20 bg-gray-900">
      <div className="max-w-6xl mx-auto text-center">
        <h2 className="text-4xl font-bold mb-10">Skills</h2>
        <div className="grid grid-cols-3 md:grid-cols-5 gap-10 text-5xl justify-items-center">
          {skills.map((s, idx) => (
            <div key={idx} className="flex flex-col items-center gap-2">
              {s.icon}
              <span className="text-base text-gray-300">{s.name}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
7️⃣ Experience.jsx
export default function Experience() {
  const experiences = [
    {
      title: "Senior Software Developer",
      company: "Norwegian Cruise Line Holdings Ltd. (NCLH)",
      period: "Aug 2024 - Sep 2025",
      desc: "Delivered backend & full-stack features supporting high-traffic websites, APIs, cloud deployments, microservices, CI/CD, and front-end frameworks."
    },
    {
      title: "Multi-Role Technical Experience",
      company: "Florida International University (FIU)",
      period: "Jan 2018 - Jul 2024",
      desc: "Worked on ERP systems, data analytics, internal tools, web platforms, UX improvements, and database management across multiple technologies."
    }
  ];

  return (
    <section id="experience" className="py-20 max-w-6xl mx-auto px-6">
      <h2 className="text-4xl font-bold text-center mb-12">Experience</h2>
      <div className="flex flex-col gap-8">
        {experiences.map((exp, idx) => (
          <div key={idx} className="bg-gray-800 p-6 rounded-xl hover:scale-105 transition">
            <h3 className="text-xl font-bold">{exp.title}</h3>
            <span className="text-gray-400">{exp.company} | {exp.period}</span>
            <p className="mt-2 text-gray-300">{exp.desc}</p>
          </div>
        ))}
      </div>
    </section>
  )
}
8️⃣ Projects.jsx
export default function Projects() {
  const projects = [
    { title: "Corporate Platform", tech: "React, Next.js, TypeScript, Tailwind, Node, Django, Redis", link: "https://www.nclhltd.com/" },
    { title: "Consumer Booking Platform", tech: "Angular, TypeScript, Node, Scala, Stripe", link: "https://www.ncl.com/" },
    { title: "AI Budgeting App", tech: "React Native, TypeScript, Firebase, gRPC, Python ML", link: "#" },
  ];

  return (
    <section id="projects" className="py-20 max-w-6xl mx-auto px-6">
      <h2 className="text-4xl font-bold text-center mb-12">Projects</h2>
      <div className="grid md:grid-cols-3 gap-8">
        {projects.map((p, idx) => (
          <div key={idx} className="bg-gray-800 p-6 rounded-xl hover:scale-105 transition">
            <h3 className="text-xl font-bold mb-2">{p.title}</h3>
            <p className="text-gray-300 mb-4">{p.tech}</p>
            <a href={p.link} target="_blank" className="bg-primary text-dark px-4 py-2 rounded-lg inline-block">
              View Project
            </a>
          </div>
        ))}
      </div>
    </section>
  )
}
9️⃣ Certifications.jsx
export default function Certifications() {
  const certs = [
    "Deep Learning Specialization - DeepLearning.AI",
    "Structuring Machine Learning Projects - DeepLearning.AI",
    "Learning Django - LinkedIn",
    "IBM Data Science Professional Certificate - Coursera"
  ];

  return (
    <section id="certifications" className="py-20 bg-gray-900">
      <div className="max-w-4xl mx-auto text-center">
        <h2 className="text-4xl font-bold mb-10">Certifications</h2>
        <ul className="flex flex-col gap-4">
          {certs.map((c, idx) => <li key={idx} className="bg-gray-800 p-4 rounded-lg">{c}</li>)}
        </ul>
      </div>
    </section>
  )
}
🔟 Contact.jsx
export default function Contact() {
  return (
    <section id="contact" className="py-20 max-w-4xl mx-auto text-center px-6">
      <h2 className="text-4xl font-bold mb-6">Contact Me</h2>
      <p className="text-gray-300 mb-4">w.bexpert5007@gmail.com | +1 (929) 810 3425 | in/stmorales23</p>
      <button className="bg-primary text-dark px-6 py-3 rounded-lg hover:scale-105 transition">
        Send Message
      </button>
    </section>
  )
}
11️⃣ App.jsx
import Navbar from "./components/Navbar";
import Hero from "./components/Hero";
import About from "./components/About";
import Skills from "./components/Skills";
import Experience from "./components/Experience";
import Projects from "./components/Projects";
import Certifications from "./components/Certifications";
import Contact from "./components/Contact";

function App() {
  return (
    <>
      <Navbar />
      <Hero />
      <About />
      <Skills />
      <Experience />
      <Projects />
      <Certifications />
      <Contact />
    </>
  );
}

export default App;
✅ Features of This Portfolio
Dark/light toggle

Smooth animations with Framer Motion

Picture in About section

Skill icons

Project cards with hover effect

Experience and Certifications clearly laid out

Fully responsive

Elegant, senior-level design

I can also generate a high-fidelity preview image of how your portfolio will look with your picture so you can see the exact design before coding.

Do you want me to do that?


give me the sample image about this code


Image created
•
Steven Morales Portfolio Website Screenshot
Generated image
Generated image
Generated image



Share

if I run this, how?

Here is the simple step-by-step process to run the portfolio I gave you. You only need Node.js installed.

1️⃣ Install Node.js
Install Node.js

Check installation:

node -v
npm -v
2️⃣ Create the React Project
Use Vite (faster than Create React App).

npm create vite@latest steven-portfolio
Choose:

Framework: React
Variant: JavaScript
Then:

cd steven-portfolio
npm install
3️⃣ Install Required Libraries
npm install tailwindcss postcss autoprefixer
npm install framer-motion react-icons react-scroll
Initialize Tailwind:

npx tailwindcss init -p
4️⃣ Replace Project Files
Open the folder in Visual Studio Code

code .
Then:

Replace files with the code I gave you:

src/
components/
Navbar.jsx
Hero.jsx
About.jsx
Skills.jsx
Experience.jsx
Projects.jsx
Certifications.jsx
Contact.jsx
Also create:

src/assets/steven.jpg
Put your picture there.

5️⃣ Run the Portfolio
Start the development server:

npm run dev
You will see something like:

Local: http://localhost:5173
Open that in your browser.

6️⃣ Result
Your portfolio will open locally like:

http://localhost:5173
Sections:

Navbar
Hero
About (with your photo)
Skills
Experience
Projects
Certifications
Contact
7️⃣ Build for Production
When ready:

npm run build
This creates:

dist/
Upload this to hosting.

8️⃣ Free Hosting Options
You can deploy easily to:

Vercel

Netlify

GitHub (GitHub Pages)

⭐ Pro Tip (Senior Portfolio)
Add later:

GitHub activity graph

Animated skill bars

Blog section

Project screenshots

Resume download button

This makes your portfolio much more impressive to recruiters.

✅ If you want, I can also give you a MUCH MORE ADVANCED portfolio (Apple / Stripe level design) with:

3D animations

glassmorphism UI

interactive project showcase

AI-level developer portfolio style

It will look 10× better than normal React portfolios.


http://localhost:5173

this doesn't play

If http://localhost:5173 is not opening/working, usually one of these is the problem. Try these quick checks:

1️⃣ Make sure the dev server is running
In your project folder, run:

npm install
npm run dev
If it's a Vite project, the terminal should show something like:

VITE v5.x.x  ready in 300 ms

➜  Local:   http://localhost:5173/
Then open that link in your browser.

2️⃣ Check the terminal for errors
If it shows errors like:

Port 5173 already in use

Failed to start dev server

Module not found

Share that error. That tells exactly what's wrong.

3️⃣ Try opening exactly this
Sometimes the slash matters:

http://localhost:5173/
4️⃣ If port 5173 is busy
Run:

npm run dev -- --port 3000
Then open:

http://localhost:3000
5️⃣ If nothing loads (blank page)
Open Browser Console:

Press F12

Go to Console

Check for red errors

✅ If you want, send me:

your project folder screenshot

the terminal output after npm run dev

…and I’ll quickly tell you exactly what's wrong.


Uncaught SyntaxError: The requested module '/node_modules/.vite/deps/react-icons_si.js?v=39bc47f8' does not provide an export named 'SiVueDotJs' (at Skills.jsx:2:34)

This error means the icon name is wrong in your import.

react-icons does not export SiVueDotJs.

❌ Your current code (likely)
import { SiVueDotJs } from "react-icons/si";
✅ Correct icon name
Use:

import { SiVuedotjs } from "react-icons/si";
Notice the difference:

❌ SiVueDotJs

✅ SiVuedotjs

The "dotjs" is lowercase.

Example working code
import { SiReact, SiVuedotjs, SiNodedotjs } from "react-icons/si";

function Skills() {
  return (
    <div>
      <SiReact />
      <SiVuedotjs />
      <SiNodedotjs />
    </div>
  );
}

export default Skills;
If it still errors
Restart Vite:

npm run dev
or clear cache:

rm -rf node_modules
npm install
npm run dev
✅ If you want, send your Skills.jsx file, and I’ll fix the imports for you in 10 seconds.


Uncaught SyntaxError: The requested module '/node_modules/.vite/deps/react-icons_si.js?v=39bc47f8' does not provide an export named 'SiVuedotJs' (at Skills.jsx:2:34)

The problem is still the icon name. The correct export from React Icons is:

SiVuedotjs
You wrote:

SiVuedotJs   ❌
The J must be lowercase.

✅ Fix
Change this in Skills.jsx
import { SiReact, SiNextdotjs, SiNodedotjs, SiVuedotjs } from "react-icons/si";
Example Working Component
import { SiReact, SiNextdotjs, SiNodedotjs, SiVuedotjs } from "react-icons/si";

function Skills() {
  return (
    <section>
      <h2>Skills</h2>

      <div style={{ display: "flex", gap: "20px", fontSize: "40px" }}>
        <SiReact />
        <SiNextdotjs />
        <SiNodedotjs />
        <SiVuedotjs />
      </div>
    </section>
  );
}

export default Skills;
🔎 Why this happens
In Vite + React, the module loader is case-sensitive.
So:

SiVuedotJs ❌
SiVuedotjs ✅
are treated as different exports.

💡 Quick tip
You can always check available icons here:

👉 https://react-icons.github.io/react-icons/icons?name=si

Search Vue and copy the exact name.

✅ If you want, I can also give you a much better portfolio layout (looks like a $5k developer portfolio) with:

hero section

animated skills

project cards

dark modern UI

fully responsive

deployable to Vercel

Just tell me 👍


import { FaReact, FaNodeJs, FaPython, FaDocker, FaAws } from "react-icons/fa";
import { SiNextdotjs, SiAngular, SiVuedotjs, SiKubernetes } from "react-icons/si";

export default function Skills() {
  const skills = [
    { icon: <FaReact />, name: "React" },
    { icon: <SiNextdotjs />, name: "Next.js" },
    { icon: <FaNodeJs />, name: "Node.js" },
    { icon: <FaPython />, name: "Python" },
    { icon: <SiAngular />, name: "Angular" },
    { icon: <SiVuedotjs />, name: "Vue.js" },
    { icon: <FaDocker />, name: "Docker" },
    { icon: <SiKubernetes />, name: "Kubernetes" },
    { icon: <FaAws />, name: "AWS" },
  ];

  return (
    <section id="skills" className="py-20 bg-gray-900">
      <div className="max-w-6xl mx-auto text-center">
        <h2 className="text-4xl font-bold mb-10">Skills</h2>
        <div className="grid grid-cols-3 md:grid-cols-5 gap-10 text-5xl justify-items-center">
          {skills.map((s, idx) => (
            <div key={idx} className="flex flex-col items-center gap-2">
              {s.icon}
              <span className="text-base text-gray-300">{s.name}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}

this is my skills.jsx code.
I am gonna give here animation (flowing skill icons)

To make flowing / animated skill icons, the best way in React is using Framer Motion.
We’ll make the icons:

float up & down

fade in

scale on hover

move slightly (smooth flowing effect)

1️⃣ Install animation library
npm install framer-motion
2️⃣ Animated Skills.jsx
Replace your file with this:

import { FaReact, FaNodeJs, FaPython, FaDocker, FaAws } from "react-icons/fa";
import { SiNextdotjs, SiAngular, SiVuedotjs, SiKubernetes } from "react-icons/si";
import { motion } from "framer-motion";

export default function Skills() {

  const skills = [
    { icon: <FaReact />, name: "React" },
    { icon: <SiNextdotjs />, name: "Next.js" },
    { icon: <FaNodeJs />, name: "Node.js" },
    { icon: <FaPython />, name: "Python" },
    { icon: <SiAngular />, name: "Angular" },
    { icon: <SiVuedotjs />, name: "Vue.js" },
    { icon: <FaDocker />, name: "Docker" },
    { icon: <SiKubernetes />, name: "Kubernetes" },
    { icon: <FaAws />, name: "AWS" },
  ];

  return (
    <section id="skills" className="py-20 bg-gray-900">
      <div className="max-w-6xl mx-auto text-center">

        <h2 className="text-4xl font-bold mb-16">Skills</h2>

        <div className="grid grid-cols-3 md:grid-cols-5 gap-14 text-5xl justify-items-center">

          {skills.map((s, idx) => (
            <motion.div
              key={idx}
              className="flex flex-col items-center gap-3 cursor-pointer"
              
              initial={{ opacity: 0, y: 40 }}
              whileInView={{ opacity: 1, y: 0 }}
              
              animate={{ y: [0, -10, 0] }}

              transition={{
                delay: idx * 0.1,
                duration: 2,
                repeat: Infinity,
                repeatType: "loop"
              }}

              whileHover={{
                scale: 1.3,
                rotate: 10
              }}
            >
              {s.icon}
              <span className="text-base text-gray-300">{s.name}</span>
            </motion.div>
          ))}

        </div>
      </div>
    </section>
  );
}
3️⃣ What animation does
Your skills will now:

✔ Float slowly
✔ Fade in when scrolling
✔ Scale when hovering
✔ Slight rotation on hover
✔ Smooth infinite motion

4️⃣ Visual effect you will see
Icons will look like:

⚛ React      ↑ ↓ floating
Next.js      ↑ ↓ floating
Node.js      ↑ ↓ floating
Python       ↑ ↓ floating
Angular      ↑ ↓ floating
When hovering:

icon → enlarge + rotate
5️⃣ If you want SUPER impressive portfolio animation
I can also add:

orbiting skills (icons rotating around center)

3D floating tech stack

particles background like Stripe

animated skill progress bars

GitHub contribution graph

These make a portfolio look 10× more professional.

If you want, I can also show you a 🔥 stunning animated skills section preview.


I wanna other style

Got it 👍
Instead of floating icons, here are 3 much cooler styles you can use for your skills section. I’ll give you the best one: a horizontal flowing tech stack slider (like modern dev portfolios).

This looks very professional and feels like skills are flowing continuously.

Style: Flowing Infinite Skill Slider
Skills move left → right continuously, like a tech banner.

Example effect:

React   Next.js   Node.js   Python   Angular   Vue   Docker   Kubernetes   AWS
React   Next.js   Node.js   Python   Angular   Vue   Docker   Kubernetes   AWS
Smooth infinite motion.

1️⃣ Install animation library
npm install framer-motion
Uses Framer Motion.

2️⃣ New Skills.jsx
import { FaReact, FaNodeJs, FaPython, FaDocker, FaAws } from "react-icons/fa";
import { SiNextdotjs, SiAngular, SiVuedotjs, SiKubernetes } from "react-icons/si";
import { motion } from "framer-motion";

export default function Skills() {

  const skills = [
    { icon: <FaReact />, name: "React" },
    { icon: <SiNextdotjs />, name: "Next.js" },
    { icon: <FaNodeJs />, name: "Node.js" },
    { icon: <FaPython />, name: "Python" },
    { icon: <SiAngular />, name: "Angular" },
    { icon: <SiVuedotjs />, name: "Vue.js" },
    { icon: <FaDocker />, name: "Docker" },
    { icon: <SiKubernetes />, name: "Kubernetes" },
    { icon: <FaAws />, name: "AWS" },
  ];

  return (
    <section id="skills" className="py-24 bg-gray-900 overflow-hidden">

      <h2 className="text-4xl font-bold text-center mb-16">Tech Stack</h2>

      <motion.div
        className="flex gap-16 text-6xl w-max"
        animate={{ x: ["0%", "-50%"] }}
        transition={{
          repeat: Infinity,
          duration: 20,
          ease: "linear"
        }}
      >

        {[...skills, ...skills].map((s, idx) => (
          <div
            key={idx}
            className="flex flex-col items-center text-gray-300 hover:text-white transition"
          >
            {s.icon}
            <span className="text-sm mt-2">{s.name}</span>
          </div>
        ))}

      </motion.div>

    </section>
  );
}
3️⃣ Result
Your skills will continuously slide across the screen.

⚛ React → Next → Node → Python → Angular → Vue → Docker → Kubernetes → AWS
Then repeat infinitely.

Hover effect highlights icons.

This style is used on many premium developer portfolios.

4️⃣ Optional Upgrade (looks MUCH better)
Add glass cards around icons:

className="flex flex-col items-center bg-gray-800 p-6 rounded-xl hover:scale-110 transition"
5️⃣ Even Better Skills Sections (Best Portfolio Styles)
If you want, I can give you much more impressive ones, like:

1️⃣ Orbiting skills (very cool)
Icons rotate around a center circle.

        React
   Node        Vue

 Python   YOU   AWS

   Docker      Next
2️⃣ 3D tech stack sphere
Skills rotate like a 3D globe.

3️⃣ Apple-style animated cards
Each skill animates on scroll.

⭐ I recommend Orbit Skills — it looks insanely good for developer portfolios.

If you want, I can give you the orbit animation code (very impressive).


Skills rotate like a 3D globe.

Perfect! A 3D rotating skill globe looks super professional and is often used by top portfolios. We can do this using three.js with react-three-fiber, which integrates Three.js with React.

Here’s a full example:

1️⃣ Install dependencies
npm install three @react-three/fiber @react-three/drei
2️⃣ Create SkillsGlobe.jsx
import React from "react";
import { Canvas } from "@react-three/fiber";
import { OrbitControls, Text } from "@react-three/drei";

export default function SkillsGlobe() {
  const skills = [
    "React", "Next.js", "Node.js", "Python", 
    "Angular", "Vue.js", "Docker", "Kubernetes", "AWS"
  ];

  // position points on a sphere
  const radius = 3;
  const points = skills.map((skill, i) => {
    const phi = Math.acos(-1 + (2 * i) / skills.length);
    const theta = Math.sqrt(skills.length * Math.PI) * phi;
    const x = radius * Math.cos(theta) * Math.sin(phi);
    const y = radius * Math.sin(theta) * Math.sin(phi);
    const z = radius * Math.cos(phi);
    return { skill, position: [x, y, z] };
  });

  return (
    <section id="skills" className="py-24 bg-gray-900">
      <h2 className="text-4xl font-bold text-center text-white mb-16">
        Tech Skills
      </h2>
      <div className="w-full h-96">
        <Canvas>
          <ambientLight intensity={0.5} />
          <pointLight position={[10, 10, 10]} />
          {points.map((p, i) => (
            <Text
              key={i}
              position={p.position}
              fontSize={0.5}
              color="white"
              anchorX="center"
              anchorY="middle"
            >
              {p.skill}
            </Text>
          ))}
          <OrbitControls autoRotate autoRotateSpeed={0.5} enableZoom={false} />
        </Canvas>
      </div>
    </section>
  );
}
3️⃣ Features
✅ Skills rotate in 3D continuously
✅ Interactive with mouse drag (via OrbitControls)
✅ Looks like a globe of skills floating in space
✅ Fully responsive

4️⃣ How to use
Just replace your old Skills.jsx with:

import SkillsGlobe from "./components/SkillsGlobe";

function App() {
  return (
    <>
      <Navbar />
      <Hero />
      <About />
      <SkillsGlobe />   {/* 3D rotating globe */}
      <Experience />
      <Projects />
      <Certifications />
      <Contact />
    </>
  );
}
5️⃣ Optional Enhancements
Add icons inside the globe using <Html> from @react-three/drei

Change autoRotateSpeed for faster rotation

Add glow or particle effects around the globe for a premium look

If you want, I can make a ready-to-use, fully styled 3D skill globe with icons exactly for your portfolio, so it looks like Stripe / Apple developer portfolio.

Do you want me to do that?


I am gonna give animation to mouse point when the mouse point moves.

Ah! You want the 3D skill globe to respond to mouse movement, so the globe rotates with the cursor — this is super interactive and looks amazing. With react-three-fiber, we can do it easily using pointer events or mouse position.

Here’s how to modify your SkillsGlobe for mouse-controlled rotation:

1️⃣ Update SkillsGlobe.jsx
import React, { useRef } from "react";
import { Canvas, useFrame } from "@react-three/fiber";
import { Text, OrbitControls } from "@react-three/drei";

function RotatingSkills({ skills }) {
  const groupRef = useRef();

  // Generate points on a sphere
  const radius = 3;
  const points = skills.map((skill, i) => {
    const phi = Math.acos(-1 + (2 * i) / skills.length);
    const theta = Math.sqrt(skills.length * Math.PI) * phi;
    const x = radius * Math.cos(theta) * Math.sin(phi);
    const y = radius * Math.sin(theta) * Math.sin(phi);
    const z = radius * Math.cos(phi);
    return { skill, position: [x, y, z] };
  });

  // Rotate group based on mouse
  useFrame(({ mouse }) => {
    if (groupRef.current) {
      groupRef.current.rotation.y = mouse.x * Math.PI;   // horizontal movement
      groupRef.current.rotation.x = mouse.y * Math.PI;   // vertical movement
    }
  });

  return (
    <group ref={groupRef}>
      {points.map((p, i) => (
        <Text
          key={i}
          position={p.position}
          fontSize={0.5}
          color="white"
          anchorX="center"
          anchorY="middle"
        >
          {p.skill}
        </Text>
      ))}
    </group>
  );
}

export default function SkillsGlobe() {
  const skills = [
    "React", "Next.js", "Node.js", "Python",
    "Angular", "Vue.js", "Docker", "Kubernetes", "AWS"
  ];

  return (
    <section id="skills" className="py-24 bg-gray-900">
      <h2 className="text-4xl font-bold text-center text-white mb-16">
        Tech Skills
      </h2>
      <div className="w-full h-96">
        <Canvas>
          <ambientLight intensity={0.5} />
          <pointLight position={[10, 10, 10]} />
          <RotatingSkills skills={skills} />
          <OrbitControls enableZoom={false} enableRotate={false} />
        </Canvas>
      </div>
    </section>
  );
}
2️⃣ How It Works
useFrame runs every frame (60 FPS)

mouse.x and mouse.y are values between -1 and 1

Multiply by Math.PI for smooth rotation

The <group> containing all skill texts rotates based on mouse movement

OrbitControls disabled rotation/zoom so only mouse pointer drives motion

3️⃣ Optional Improvements
Add slight auto-rotation so it still spins when mouse is idle:

useFrame(({ mouse, clock }) => {
  if (groupRef.current) {
    groupRef.current.rotation.y = mouse.x * Math.PI + clock.getElapsedTime() * 0.2;
    groupRef.current.rotation.x = mouse.y * Math.PI + Math.sin(clock.getElapsedTime() * 0.3) * 0.1;
  }
});
Add hover effects on each skill using <Html> or color change

Make globe responsive: adjust radius based on screen size

If you want, I can give you a ready-to-use, fully interactive 3D skill globe with your icons floating, rotating with mouse, glowing effect, and modern styling — basically portfolio-ready premium style.

Do you want me to do that?


and I am gonna give the animation of Fire to mouse pointer when it is moving.

Ah, now we’re talking next-level interactive portfolio stuff 😎🔥

You want a fire/flame effect following the mouse pointer on top of your portfolio — this can be done with particles using react-three-fiber + @react-three/drei or even a lightweight HTML canvas particle system.

I’ll give you a React + Tailwind + Three.js/Canvas fire pointer effect that works with your 3D skills globe.

Option 1: Simple HTML Canvas Fire Pointer
This method is easy, lightweight, works anywhere:

// src/components/FirePointer.jsx
import { useEffect } from "react";

export default function FirePointer() {

  useEffect(() => {
    const canvas = document.createElement("canvas");
    document.body.appendChild(canvas);
    canvas.style.position = "fixed";
    canvas.style.top = 0;
    canvas.style.left = 0;
    canvas.style.pointerEvents = "none"; // don't block clicks
    canvas.style.zIndex = 9999;
    const ctx = canvas.getContext("2d");

    let width = window.innerWidth;
    let height = window.innerHeight;
    canvas.width = width;
    canvas.height = height;

    const particles = [];
    const colors = ["#ff4500", "#ff8c00", "#ffd700", "#fff"];

    function createParticle(x, y) {
      return {
        x,
        y,
        vx: (Math.random() - 0.5) * 2,
        vy: -Math.random() * 2 - 1,
        size: Math.random() * 5 + 2,
        color: colors[Math.floor(Math.random() * colors.length)],
        life: 50 + Math.random() * 30
      };
    }

    function animate() {
      ctx.clearRect(0, 0, width, height);
      particles.forEach((p, i) => {
        p.x += p.vx;
        p.y += p.vy;
        p.size *= 0.95;
        p.life--;
        ctx.fillStyle = p.color;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
        if (p.life <= 0 || p.size <= 0.5) particles.splice(i, 1);
      });
      requestAnimationFrame(animate);
    }

    function handleMouseMove(e) {
      for (let i = 0; i < 5; i++) {
        particles.push(createParticle(e.clientX, e.clientY));
      }
    }

    window.addEventListener("mousemove", handleMouseMove);
    animate();

    // resize
    window.addEventListener("resize", () => {
      width = window.innerWidth;
      height = window.innerHeight;
      canvas.width = width;
      canvas.height = height;
    });

    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      document.body.removeChild(canvas);
    };
  }, []);

  return null;
}
2️⃣ Use it in your App
import FirePointer from "./components/FirePointer";
import Navbar from "./components/Navbar";
import Hero from "./components/Hero";
import About from "./components/About";
import SkillsGlobe from "./components/SkillsGlobe";
import Experience from "./components/Experience";
import Projects from "./components/Projects";
import Certifications from "./components/Certifications";
import Contact from "./components/Contact";

function App() {
  return (
    <>
      <FirePointer />
      <Navbar />
      <Hero />
      <About />
      <SkillsGlobe />
      <Experience />
      <Projects />
      <Certifications />
      <Contact />
    </>
  );
}

export default App;
✅ Features
🔥 Fire particle effect follows the mouse

🎨 Glowing orange/red/yellow fire colors

🖱 Works on top of your 3D globe and portfolio sections

💨 Lightweight, smooth 60FPS animation

✨ Can customize: particle size, colors, speed

Optional Enhancements
Make particles trail fade out with glow effect using ctx.shadowBlur = 15; ctx.shadowColor = p.color;

Use Three.js particles for more realistic fire

Combine with SkillsGlobe rotation → cursor moves both fire & globe

If you want, I can make a full demo combining:

🔥 Fire pointer following mouse

🌐 Interactive 3D rotating skill globe

✨ Glowing skill icons

🚀 Modern senior portfolio layout

So it looks like a premium developer portfolio ready to deploy.

Do you want me to do that?


and I am gonna convert mouse pointer style as fire sword

Ahhhh 😎🔥 You want a full “fire sword” cursor — like a glowing fiery trail that follows the mouse, not just particles. This is totally doable in React, and it looks amazing for a portfolio.

We can build it using HTML canvas, then draw a sword-shaped trail with fire particles following the mouse. Here’s a ready-to-use solution.

1️⃣ Fire Sword Cursor Component
// src/components/FireSwordCursor.jsx
import { useEffect } from "react";

export default function FireSwordCursor() {
  useEffect(() => {
    const canvas = document.createElement("canvas");
    document.body.appendChild(canvas);
    const ctx = canvas.getContext("2d");

    canvas.style.position = "fixed";
    canvas.style.top = 0;
    canvas.style.left = 0;
    canvas.style.pointerEvents = "none";
    canvas.style.zIndex = 9999;

    let width = window.innerWidth;
    let height = window.innerHeight;
    canvas.width = width;
    canvas.height = height;

    const particles = [];
    const colors = ["#ff4500", "#ff8c00", "#ffd700", "#fff"];

    let mouse = { x: width / 2, y: height / 2 };

    function createParticle(x, y) {
      return {
        x,
        y,
        vx: (Math.random() - 0.5) * 2,
        vy: -Math.random() * 2 - 1,
        size: Math.random() * 4 + 2,
        color: colors[Math.floor(Math.random() * colors.length)],
        life: 40 + Math.random() * 20,
      };
    }

    function drawSword() {
      // Sword base line
      ctx.beginPath();
      ctx.moveTo(mouse.x, mouse.y);
      ctx.lineTo(mouse.x, mouse.y - 40); // sword length
      ctx.lineWidth = 4;
      ctx.strokeStyle = "orange";
      ctx.shadowBlur = 15;
      ctx.shadowColor = "orange";
      ctx.stroke();

      // Fire particles along sword
      for (let i = 0; i < 3; i++) {
        particles.push(createParticle(mouse.x, mouse.y - Math.random() * 40));
      }
    }

    function animate() {
      ctx.clearRect(0, 0, width, height);
      drawSword();

      // draw particles
      particles.forEach((p, i) => {
        p.x += p.vx;
        p.y += p.vy;
        p.size *= 0.95;
        p.life--;
        ctx.fillStyle = p.color;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
        if (p.life <= 0 || p.size <= 0.5) particles.splice(i, 1);
      });

      requestAnimationFrame(animate);
    }

    animate();

    const handleMouseMove = (e) => {
      mouse.x = e.clientX;
      mouse.y = e.clientY;
    };

    window.addEventListener("mousemove", handleMouseMove);

    // resize
    window.addEventListener("resize", () => {
      width = window.innerWidth;
      height = window.innerHeight;
      canvas.width = width;
      canvas.height = height;
    });

    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      document.body.removeChild(canvas);
    };
  }, []);

  return null;
}
2️⃣ Use it in App
import FireSwordCursor from "./components/FireSwordCursor";
import Navbar from "./components/Navbar";
import Hero from "./components/Hero";
import About from "./components/About";
import SkillsGlobe from "./components/SkillsGlobe";

function App() {
  return (
    <>
      <FireSwordCursor />   {/* 🔥 Fire sword cursor */}
      <Navbar />
      <Hero />
      <About />
      <SkillsGlobe />
    </>
  );
}

export default App;
3️⃣ Features
🔥 Sword-shaped cursor with glowing particles

✨ Fire trail flows behind the sword

🖱 Cursor completely replaces default pointer

🎨 Fully customizable: colors, sword length, particle speed, glow

4️⃣ Optional Upgrades
Glow effect: Increase ctx.shadowBlur and use gradient colors

Sword animation: Add rotation with mouse movement (slashing effect)

Particle randomness: More particles for heavier fire trail

Integration with 3D Skills Globe: Fire sword moves over globe

If you want, I can make a full portfolio demo with:

🖱 Fire sword cursor

🌐 3D rotating skill globe responding to mouse

🔥 Premium glowing particle animation

✨ Modern portfolio layout

It will look like a $5k developer portfolio.

Do you want me to create that full demo?


then yet, mouse pointer is also an arrow and on it, sword appears. 
It's not that I am hoping.
instead of cursor, firesword

Ah! Now I understand perfectly 😎

You don’t want the default arrow pointer at all. You want the fire sword itself to be the cursor, so the arrow disappears, and only the sword with fire is visible and follows the mouse.

We can do this by:

Hiding the default cursor with CSS.

Using the FireSword component to follow the mouse as the new cursor.

Here’s the fully corrected version:

1️⃣ Hide default cursor globally
In index.css (or Tailwind global.css):

/* hide default cursor */
body {
  cursor: none;
}
2️⃣ Fire Sword Cursor Component
// src/components/FireSwordCursor.jsx
import { useEffect } from "react";

export default function FireSwordCursor() {
  useEffect(() => {
    const canvas = document.createElement("canvas");
    document.body.appendChild(canvas);
    const ctx = canvas.getContext("2d");

    canvas.style.position = "fixed";
    canvas.style.top = 0;
    canvas.style.left = 0;
    canvas.style.pointerEvents = "none"; // don't block clicks
    canvas.style.zIndex = 9999;

    let width = window.innerWidth;
    let height = window.innerHeight;
    canvas.width = width;
    canvas.height = height;

    const particles = [];
    const colors = ["#ff4500", "#ff8c00", "#ffd700", "#fff"];
    let mouse = { x: width / 2, y: height / 2 };

    function createParticle(x, y) {
      return {
        x,
        y,
        vx: (Math.random() - 0.5) * 2,
        vy: -Math.random() * 2 - 1,
        size: Math.random() * 4 + 2,
        color: colors[Math.floor(Math.random() * colors.length)],
        life: 40 + Math.random() * 20,
      };
    }

    function drawSword() {
      // Draw sword as a glowing line
      ctx.beginPath();
      ctx.moveTo(mouse.x, mouse.y);
      ctx.lineTo(mouse.x, mouse.y - 40);
      ctx.lineWidth = 6;
      ctx.strokeStyle = "orange";
      ctx.shadowBlur = 20;
      ctx.shadowColor = "orange";
      ctx.stroke();

      // fire particles along sword
      for (let i = 0; i < 4; i++) {
        particles.push(createParticle(mouse.x, mouse.y - Math.random() * 40));
      }
    }

    function animate() {
      ctx.clearRect(0, 0, width, height);
      drawSword();

      particles.forEach((p, i) => {
        p.x += p.vx;
        p.y += p.vy;
        p.size *= 0.95;
        p.life--;
        ctx.fillStyle = p.color;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
        if (p.life <= 0 || p.size <= 0.5) particles.splice(i, 1);
      });

      requestAnimationFrame(animate);
    }

    animate();

    const handleMouseMove = (e) => {
      mouse.x = e.clientX;
      mouse.y = e.clientY;
    };

    window.addEventListener("mousemove", handleMouseMove);

    // Resize canvas
    window.addEventListener("resize", () => {
      width = window.innerWidth;
      height = window.innerHeight;
      canvas.width = width;
      canvas.height = height;
    });

    return () => {
      window.removeEventListener("mousemove", handleMouseMove);
      document.body.removeChild(canvas);
    };
  }, []);

  return null;
}
3️⃣ Use it in your App
import FireSwordCursor from "./components/FireSwordCursor";
import Navbar from "./components/Navbar";
import Hero from "./components/Hero";
import About from "./components/About";
import SkillsGlobe from "./components/SkillsGlobe";

function App() {
  return (
    <>
      <FireSwordCursor /> {/* 🔥 Fire sword replaces cursor */}
      <Navbar />
      <Hero />
      <About />
      <SkillsGlobe />
    </>
  );
}

export default App;
✅ Features Now
🖱 Default arrow pointer is gone

🔥 Fire sword line becomes the cursor

✨ Glowing particles trail the sword

🎨 Fully interactive and follows mouse smoothly

💨 Works across all portfolio sections (3D globe, hero, etc.)

Optional Enhancements
Make sword rotate slightly toward mouse movement like a real swinging sword

Make particles more fiery with gradient blur

Combine with 3D skills globe rotation → fully interactive

If you want, I can make a ready-to-run full portfolio with:

🔥 Fire sword cursor (replacing default pointer)

🌐 3D rotating skill globe (follows mouse)

✨ Glowing skill icons and modern layout

It will be professional senior developer portfolio ready to deploy.

Do you want me to create that full version?


I am gonna set bg of about page

Nice 👍 Adding a background to the About page can make your portfolio look much more premium. Since you’re using React + Tailwind, here are some excellent styles you can choose from.

1️⃣ Image Background (Most common for About page)
Example with your photo + background image.

// src/components/About.jsx

export default function About() {
  return (
    <section
      id="about"
      className="min-h-screen flex items-center justify-center bg-cover bg-center relative"
      style={{
        backgroundImage:
          "url('/about-bg.jpg')"
      }}
    >
      {/* Dark overlay */}
      <div className="absolute inset-0 bg-black/70"></div>

      <div className="relative max-w-6xl mx-auto grid md:grid-cols-2 gap-10 items-center px-6 text-white">
        
        {/* Profile image */}
        <div className="flex justify-center">
          <img
            src="/profile.jpg"
            alt="Steven Morales"
            className="w-72 rounded-2xl shadow-2xl border-4 border-orange-500"
          />
        </div>

        {/* Text */}
        <div>
          <h2 className="text-4xl font-bold mb-4">About Me</h2>

          <p className="text-gray-300 leading-relaxed">
            I'm <span className="text-orange-400 font-semibold">Steven Morales</span>, 
            a Senior Full Stack Engineer with 9+ years of experience building 
            scalable web, cloud, and mobile applications.
          </p>

          <p className="mt-4 text-gray-300">
            My expertise includes React, Next.js, Node.js, Python, AWS, 
            microservices, and modern DevOps practices.
          </p>

          <button className="mt-6 px-6 py-3 bg-orange-500 hover:bg-orange-600 rounded-lg font-semibold">
            Download Resume
          </button>
        </div>
      </div>
    </section>
  );
}
2️⃣ Animated Gradient Background (Very modern)
If you want something more stylish:

<section className="min-h-screen flex items-center justify-center bg-gradient-to-r from-gray-900 via-black to-gray-900">
Or animated:

/* index.css */

@keyframes gradientMove {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.animated-bg {
  background: linear-gradient(-45deg,#111,#1a1a1a,#ff4500,#000);
  background-size: 400% 400%;
  animation: gradientMove 10s ease infinite;
}
Use it:

<section className="animated-bg min-h-screen flex items-center justify-center">
3️⃣ Tech Pattern Background (Best for developers)
Use subtle tech background:

/public/bg-tech.jpg
Then:

style={{ backgroundImage: "url('/bg-tech.jpg')" }}
With blur overlay:

<div className="absolute inset-0 backdrop-blur-sm bg-black/60"></div>
4️⃣ Particle Background (Most impressive)
This is very popular for developer portfolios.

Install:

npm install react-tsparticles
Then you can create animated particles behind your about section.

⭐ My Recommendation for Your Portfolio
Best combination:

✔ Tech background image
✔ Dark overlay
✔ Profile picture card
✔ Glow border

It will look very professional for a Senior Engineer portfolio.

💡 If you want, I can also give you a very beautiful About page layout with:

glowing profile card

animated background

skill badges

stats (9+ years experience, 30+ projects)

hover animations

It will look like a $3000 portfolio website.


import stevenPic from "../assets/steven.jpg";
import { motion } from "framer-motion";

export default function About() {
  return (
    <section id="about" className="py-20 max-w-6xl mx-auto flex flex-col md:flex-row items-center gap-10 px-6">

      {/* Profile Image with Gradient Border */}
      <motion.div
        className="relative w-64 h-64 rounded-full p-1 bg-gradient-to-tr from-purple-500 via-pink-500 to-yellow-400"
        initial={{ opacity: 0, x: -100 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 1 }}
      >
        <div className="w-full h-full rounded-full overflow-hidden bg-gray-900">
          <img
            src={stevenPic}
            alt="Steven Morales"
            className="w-full h-full object-cover"
          />
        </div>
      </motion.div>

      {/* About Text */}
      <motion.div
        initial={{ opacity: 0, x: 100 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 1 }}
      >
        <h2 className="text-4xl font-bold mb-4 text-white">About Me</h2>
        <p className="text-gray-200 text-lg mb-4">
          Versatile and results-driven Senior Full Stack Engineer with 9 years of experience delivering scalable, performant, and secure web, cloud, and mobile solutions.
        </p>
        <p className="text-gray-200 text-lg">
          Skilled in React, Next.js, Node.js, Python, Django, Angular, Vue, Java(Spring Boot), .NET/C#, PHP, Laravel, AI/ML, Cloud(AWS/GCP), DevOps, and more.
        </p>
      </motion.div>

    </section>
  )
}
this is my about.jsx.
add to this.

Your current About.jsx is already good 👍
Let’s upgrade it into a premium section with:

🌌 animated gradient background

✨ glow particles

📊 experience stats

🎯 hover animations

🔥 better layout

Below is an enhanced version while keeping your code structure.

Improved About.jsx
import stevenPic from "../assets/steven.jpg";
import { motion } from "framer-motion";

export default function About() {
  return (
    <section
      id="about"
      className="relative py-28 px-6 overflow-hidden"
    >

      {/* Background Gradient */}
      <div className="absolute inset-0 bg-gradient-to-br from-black via-gray-900 to-black"></div>

      {/* Glow circles */}
      <div className="absolute top-20 left-10 w-72 h-72 bg-purple-500 opacity-20 blur-3xl rounded-full"></div>
      <div className="absolute bottom-20 right-10 w-72 h-72 bg-pink-500 opacity-20 blur-3xl rounded-full"></div>

      <div className="relative max-w-6xl mx-auto flex flex-col md:flex-row items-center gap-14">

        {/* Profile Image */}
        <motion.div
          className="relative w-64 h-64 rounded-full p-[3px] bg-gradient-to-tr from-purple-500 via-pink-500 to-yellow-400"
          initial={{ opacity: 0, x: -120 }}
          whileInView={{ opacity: 1, x: 0 }}
          transition={{ duration: 1 }}
        >
          <div className="w-full h-full rounded-full overflow-hidden bg-gray-900 shadow-2xl">
            <img
              src={stevenPic}
              alt="Steven Morales"
              className="w-full h-full object-cover hover:scale-110 transition duration-500"
            />
          </div>
        </motion.div>

        {/* Text Section */}
        <motion.div
          className="text-center md:text-left"
          initial={{ opacity: 0, x: 120 }}
          whileInView={{ opacity: 1, x: 0 }}
          transition={{ duration: 1 }}
        >

          <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
            About Me
          </h2>

          <p className="text-gray-300 text-lg mb-4 leading-relaxed">
            I'm <span className="text-purple-400 font-semibold">Steven Morales</span>,
            a versatile Senior Full Stack Engineer with 9+ years of experience
            building scalable web, cloud, and mobile applications.
          </p>

          <p className="text-gray-400 text-lg leading-relaxed mb-8">
            My expertise includes React, Next.js, Node.js, Python, Django,
            Angular, Vue, Java (Spring Boot), .NET/C#, DevOps, AI/ML, and
            cloud platforms like AWS and GCP.
          </p>

          {/* Stats */}
          <div className="grid grid-cols-3 gap-6 mt-6">

            <div className="bg-gray-800 p-4 rounded-xl hover:bg-gray-700 transition">
              <h3 className="text-2xl font-bold text-purple-400">9+</h3>
              <p className="text-gray-400 text-sm">Years Experience</p>
            </div>

            <div className="bg-gray-800 p-4 rounded-xl hover:bg-gray-700 transition">
              <h3 className="text-2xl font-bold text-purple-400">50+</h3>
              <p className="text-gray-400 text-sm">Projects</p>
            </div>

            <div className="bg-gray-800 p-4 rounded-xl hover:bg-gray-700 transition">
              <h3 className="text-2xl font-bold text-purple-400">20+</h3>
              <p className="text-gray-400 text-sm">Technologies</p>
            </div>

          </div>

        </motion.div>

      </div>
    </section>
  );
}
What this adds ✨
1️⃣ Modern animated background
gradient + glow blobs

2️⃣ Profile image animation
hover zoom

gradient border

3️⃣ Stats cards
Example:

9+ Years Experience

50+ Projects

20+ Technologies

4️⃣ Smooth scroll animations
(using framer-motion)

Result layout
------------------------------------------
        Glow Background

   (Profile Image)      About Text

                       Steven Morales
                       Full Stack Engineer

                       Description...

         [9+ Years] [50+ Projects] [20+ Tech]

------------------------------------------
Optional (Looks VERY premium)
I can also add:

🔥 AI-style animated background
🌌 3D floating tech icons
⚡ parallax scrolling effect
🧠 skills visualization
🪐 interactive dev universe theme

Your portfolio would look like top-tier Silicon Valley developer sites.

If you want, I can show you the #1 most impressive About section design for developer portfolios.


export default function Experience() {
  const experiences = [
    {
      title: "Senior Software Developer",
      company: "Norwegian Cruise Line Holdings Ltd. (NCLH)",
      period: "Aug 2024 - Sep 2025",
      desc: "Delivered backend & full-stack features supporting high-traffic websites, APIs, cloud deployments, microservices, CI/CD, and front-end frameworks."
    },
    {
      title: "Multi-Role Technical Experience",
      company: "Florida International University (FIU)",
      period: "Jan 2018 - Jul 2024",
      desc: "Worked on ERP systems, data analytics, internal tools, web platforms, UX improvements, and database management across multiple technologies."
    }
  ];

  return (
    <section id="experience" className="py-20 max-w-6xl mx-auto px-6">
      <h2 className="text-4xl font-bold text-center mb-12">Experience</h2>
      <div className="flex flex-col gap-8">
        {experiences.map((exp, idx) => (
          <div key={idx} className="bg-gray-800 p-6 rounded-xl hover:scale-105 transition">
            <h3 className="text-xl font-bold">{exp.title}</h3>
            <span className="text-gray-400">{exp.company} | {exp.period}</span>
            <p className="mt-2 text-gray-300">{exp.desc}</p>
          </div>
        ))}
      </div>
    </section>
  )
}

this is my experience.jsx code.
I am also gonna set bg at experience page.

Good 👍 Let’s upgrade your Experience section with:

🌌 background gradient

✨ glow blobs

📍 timeline style (much better for resumes)

🎬 scroll animation

🎨 glass cards

This style is very common in senior developer portfolios.

Improved Experience.jsx
import { motion } from "framer-motion";

export default function Experience() {

  const experiences = [
    {
      title: "Senior Software Developer",
      company: "Norwegian Cruise Line Holdings Ltd. (NCLH)",
      period: "Aug 2024 - Sep 2025",
      desc: "Delivered backend & full-stack features supporting high-traffic websites, APIs, cloud deployments, microservices, CI/CD pipelines, and modern frontend frameworks."
    },
    {
      title: "Multi-Role Technical Experience",
      company: "Florida International University (FIU)",
      period: "Jan 2018 - Jul 2024",
      desc: "Worked on ERP systems, enterprise databases, internal platforms, analytics dashboards, and modern web apps while improving UX, performance, and data reliability."
    }
  ];

  return (
    <section id="experience" className="relative py-28 px-6 overflow-hidden">

      {/* Background */}
      <div className="absolute inset-0 bg-gradient-to-b from-black via-gray-900 to-black"></div>

      {/* Glow effect */}
      <div className="absolute top-20 left-20 w-72 h-72 bg-purple-500 opacity-20 blur-3xl rounded-full"></div>
      <div className="absolute bottom-20 right-20 w-72 h-72 bg-blue-500 opacity-20 blur-3xl rounded-full"></div>

      <div className="relative max-w-6xl mx-auto">

        {/* Title */}
        <h2 className="text-4xl md:text-5xl font-bold text-center text-white mb-16">
          Experience
        </h2>

        {/* Timeline */}
        <div className="relative border-l border-gray-700">

          {experiences.map((exp, idx) => (
            <motion.div
              key={idx}
              className="mb-12 ml-8"
              initial={{ opacity: 0, x: -80 }}
              whileInView={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.7 }}
            >

              {/* Timeline Dot */}
              <span className="absolute -left-3 flex items-center justify-center w-6 h-6 bg-purple-500 rounded-full ring-8 ring-black"></span>

              {/* Card */}
              <div className="bg-gray-800/60 backdrop-blur-md p-6 rounded-xl shadow-lg hover:scale-105 transition">

                <h3 className="text-xl font-bold text-white">
                  {exp.title}
                </h3>

                <span className="text-gray-400 text-sm">
                  {exp.company} | {exp.period}
                </span>

                <p className="mt-3 text-gray-300 leading-relaxed">
                  {exp.desc}
                </p>

              </div>

            </motion.div>
          ))}

        </div>

      </div>

    </section>
  );
}
What this improves ✨
1️⃣ Timeline layout
Instead of stacked cards:

• 2025  Senior Developer
|
• 2024
|
• 2018  FIU Experience
Looks more professional for resumes.

2️⃣ Animated scroll appearance
Cards slide in when you scroll.

Uses:

framer-motion
3️⃣ Modern background
gradient background

glow blobs

glassmorphism cards

4️⃣ Hover interaction
Cards slightly scale on hover.

Result Layout
             Experience

        │
        ● Senior Software Developer
        │ Norwegian Cruise Line
        │ Aug 2024 – Sep 2025
        │ description
        │
        ● FIU Technical Roles
        │ Jan 2018 – Jul 2024
        │ description
Optional (Next level)
I can also make your experience section WAY more impressive with:

🔥 animated company logos
📊 tech stack tags for each job
🌐 interactive timeline scroll
⚡ progress career graph
🎯 GitHub project links

It will look like a Silicon Valley engineer portfolio.

If you want, I can also show you the most impressive portfolio section: Projects showcase with 3D hover cards (this is what recruiters notice first).


import React, { useRef } from "react";
import { Canvas, useFrame } from "@react-three/fiber";
import { Text, OrbitControls } from "@react-three/drei";

function RotatingSkills({ skills }) {
  const groupRef = useRef();

  // Generate points on a sphere
  const radius = 3;
  const points = skills.map((skill, i) => {
    const phi = Math.acos(-1 + (2 * i) / skills.length);
    const theta = Math.sqrt(skills.length * Math.PI) * phi;
    const x = radius * Math.cos(theta) * Math.sin(phi);
    const y = radius * Math.sin(theta) * Math.sin(phi);
    const z = radius * Math.cos(phi);
    return { skill, position: [x, y, z] };
  });

  // Rotate group based on mouse
  useFrame(({ mouse, clock }) => {
  if (groupRef.current) {
    groupRef.current.rotation.y = mouse.x * Math.PI + clock.getElapsedTime() * 0.2;
    groupRef.current.rotation.x = mouse.y * Math.PI + Math.sin(clock.getElapsedTime() * 0.3) * 0.1;
  }
});

  return (
    <group ref={groupRef}>
      {points.map((p, i) => (
        <Text
          key={i}
          position={p.position}
          fontSize={0.5}
          color="white"
          anchorX="center"
          anchorY="middle"
        >
          {p.skill}
        </Text>
      ))}
    </group>
  );
}

export default function SkillsGlobe() {
  const skills = [
    "React", "Next.js", "Node.js", "Python",
    "Angular", "Vue.js", "Docker", "Kubernetes", "AWS"
  ];

  return (
    <section id="skills" className="py-24 bg-gray-900">
      <h2 className="text-4xl font-bold text-center text-white mb-16">
        Tech Skills
      </h2>
      <div className="w-full h-96">
        <Canvas>
          <ambientLight intensity={0.5} />
          <pointLight position={[10, 10, 10]} />
          <RotatingSkills skills={skills} />
          <OrbitControls enableZoom={false} enableRotate={false} />
        </Canvas>
      </div>
    </section>
  );
}

this is my skills.jsx code.
I wanna insert bg image that is moving, that u are offering,  to the bg of this part.

You can add a moving background image (parallax / animated background) behind your 3D skills globe while keeping the React Three Fiber canvas on top.

The trick is:

Make the section relative

Add an absolute background layer

Animate the background with CSS

1️⃣ Update your SkillsGlobe section
Modify your component like this:

export default function SkillsGlobe() {
  const skills = [
    "React", "Next.js", "Node.js", "Python",
    "Angular", "Vue.js", "Docker", "Kubernetes", "AWS"
  ];

  return (
    <section id="skills" className="relative py-24 overflow-hidden">

      {/* Moving Background */}
      <div className="absolute inset-0 bg-moving opacity-40"></div>

      {/* Dark overlay */}
      <div className="absolute inset-0 bg-black/70"></div>

      {/* Content */}
      <div className="relative z-10">
        <h2 className="text-4xl font-bold text-center text-white mb-16">
          Tech Skills
        </h2>

        <div className="w-full h-96">
          <Canvas>
            <ambientLight intensity={0.5} />
            <pointLight position={[10, 10, 10]} />
            <RotatingSkills skills={skills} />
            <OrbitControls enableZoom={false} enableRotate={false} />
          </Canvas>
        </div>
      </div>

    </section>
  );
}
2️⃣ Add moving background animation
Add this to index.css or global.css:

.bg-moving {
  background-image: url("/tech-bg.jpg");
  background-size: cover;
  background-position: center;
  animation: moveBg 40s linear infinite;
}

@keyframes moveBg {
  0% {
    background-position: 0% 0%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 0%;
  }
}
Put your image here:

public/tech-bg.jpg
3️⃣ Even cooler version (recommended)
Instead of a normal image, use a tech / particles background.

Example images:

tech grid

neural network

galaxy

circuit board

These look very good behind 3D text.

Result
Your section will look like:

---------------------------------
     moving tech background

           Tech Skills

            (3D globe)
         React  Node  AWS
         Python Vue Angular

---------------------------------
🌌 background slowly moves

🌐 skills rotate in 3D

🖱 globe reacts to mouse

Very modern portfolio style.

💡 If you want, I can also show you an insane upgrade for this section:

🌍 real 3D globe

🧠 skills orbiting like satellites

✨ particles background

🖱 mouse interaction

This is the type of portfolio that gets recruiters' attention immediately.


add to my skills.jsx

Here is your updated Skills.jsx with a moving background image integrated directly.
This keeps your 3D globe, mouse interaction, and adds a slow animated tech background behind it.

Updated Skills.jsx
import React, { useRef } from "react";
import { Canvas, useFrame } from "@react-three/fiber";
import { Text, OrbitControls } from "@react-three/drei";

function RotatingSkills({ skills }) {
  const groupRef = useRef();

  const radius = 3;

  const points = skills.map((skill, i) => {
    const phi = Math.acos(-1 + (2 * i) / skills.length);
    const theta = Math.sqrt(skills.length * Math.PI) * phi;

    const x = radius * Math.cos(theta) * Math.sin(phi);
    const y = radius * Math.sin(theta) * Math.sin(phi);
    const z = radius * Math.cos(phi);

    return { skill, position: [x, y, z] };
  });

  useFrame(({ mouse, clock }) => {
    if (groupRef.current) {
      groupRef.current.rotation.y =
        mouse.x * Math.PI + clock.getElapsedTime() * 0.2;

      groupRef.current.rotation.x =
        mouse.y * Math.PI + Math.sin(clock.getElapsedTime() * 0.3) * 0.1;
    }
  });

  return (
    <group ref={groupRef}>
      {points.map((p, i) => (
        <Text
          key={i}
          position={p.position}
          fontSize={0.5}
          color="white"
          anchorX="center"
          anchorY="middle"
        >
          {p.skill}
        </Text>
      ))}
    </group>
  );
}

export default function SkillsGlobe() {
  const skills = [
    "React",
    "Next.js",
    "Node.js",
    "Python",
    "Angular",
    "Vue.js",
    "Docker",
    "Kubernetes",
    "AWS",
  ];

  return (
    <section
      id="skills"
      className="relative py-24 overflow-hidden"
    >
      {/* Moving Background */}
      <div className="absolute inset-0 bg-moving"></div>

      {/* Dark Overlay */}
      <div className="absolute inset-0 bg-black/70"></div>

      {/* Content */}
      <div className="relative z-10">
        <h2 className="text-4xl font-bold text-center text-white mb-16">
          Tech Skills
        </h2>

        <div className="w-full h-96">
          <Canvas>
            <ambientLight intensity={0.5} />
            <pointLight position={[10, 10, 10]} />
            <RotatingSkills skills={skills} />
            <OrbitControls enableZoom={false} enableRotate={false} />
          </Canvas>
        </div>
      </div>
    </section>
  );
}
Add Moving Background Animation
Add this in index.css or global.css

.bg-moving {
  background-image: url("/tech-bg.jpg");
  background-size: cover;
  background-position: center;
  animation: moveBackground 35s linear infinite;
  opacity: 0.35;
}

@keyframes moveBackground {
  0% {
    background-position: 0% 0%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 0%;
  }
}
Where to put the background image
Place your image here:

/public/tech-bg.jpg
Good background ideas:

tech grid

AI neural network

space galaxy

circuit board

Final Result
Your Skills section will have:

✔ moving tech background
✔ dark overlay for readability
✔ 3D rotating skill globe
✔ mouse interactive movement

Very modern developer portfolio style.

💡 If you want, I can also show you a much more impressive skills globe:

🌍 real 3D sphere

✨ glowing skill logos

🌌 particles background

🖱 magnetic mouse interaction

It will look like a top 1% developer portfolio.


the bg converted as black, but I wanted moving bg.

Your background became black because the overlay is covering the image.

In your code you probably have this:

<div className="absolute inset-0 bg-black/70"></div>
bg-black/70 = 70% black, so it hides most of the background image.

Fix 1 (Recommended)
Reduce the overlay opacity.

Change this:

<div className="absolute inset-0 bg-black/70"></div>
to

<div className="absolute inset-0 bg-black/40"></div>
or even:

<div className="absolute inset-0 bg-black/20"></div>
Fix 2 (Check your background class)
Make sure your CSS is correct:

.bg-moving {
  background-image: url("/tech-bg.jpg");
  background-size: cover;
  background-position: center;
  animation: moveBackground 35s linear infinite;
  opacity: 0.6;
}

@keyframes moveBackground {
  0% {
    background-position: 0% 0%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 0%;
  }
}
Fix 3 (Correct folder)
Make sure the image is here:

/public/tech-bg.jpg
NOT:

src/assets
Because /public works directly with:

url("/tech-bg.jpg")
Correct structure
Your section should look like this:

<section className="relative py-24 overflow-hidden">

  {/* moving image */}
  <div className="absolute inset-0 bg-moving"></div>

  {/* light overlay */}
  <div className="absolute inset-0 bg-black/30"></div>

  {/* content */}
  <div className="relative z-10">
    ...
  </div>

</section>
Result
Now you will see:

✨ moving background image
🌑 light dark overlay
🌍 3D rotating skills globe

If you want, I can also show you a MUCH better background used in top portfolios:

🔥 animated particles background (not just image)
🌌 looks like AI / galaxy / neural network
⚡ super smooth with Three.js

It will make your Skills section look 10× more professional.


give me a sample image about tech-bg.jpg


Image created
•
Futuristic glowing network of light
Generated image
Generated image
Generated image



Share



